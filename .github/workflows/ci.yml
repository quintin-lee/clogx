name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        build_type: [makefile, cmake]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build with Makefile
        if: matrix.build_type == 'makefile'
        run: make

      - name: Run tests with Makefile
        if: matrix.build_type == 'makefile'
        run: make test

      - name: Configure with CMake
        if: matrix.build_type == 'cmake'
        run: cmake -S . -B build -DCMAKE_EXPORT_COMPILE_COMMANDS=ON

      - name: Build with CMake
        if: matrix.build_type == 'cmake'
        run: cmake --build build -j

      - name: Run tests with CMake
        if: matrix.build_type == 'cmake'
        run: ctest --test-dir build --output-on-failure

      - name: Verify compile_commands.json
        if: matrix.build_type == 'cmake'
        run: test -f build/compile_commands.json

  asan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build and test with ASan
        run: |
          make clean
          make test CC=clang CFLAGS='-std=c99 -Wall -Wextra -Iinclude -O2 -D_GNU_SOURCE -fsanitize=address -fno-omit-frame-pointer'

  valgrind:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build with Makefile
        run: make

      - name: Install valgrind
        run: sudo apt-get update && sudo apt-get install -y valgrind

      - name: Run tests under valgrind
        run: |
          valgrind --error-exitcode=1 --leak-check=full --track-origins=yes ./build/test_async_lifecycle
          valgrind --error-exitcode=1 --leak-check=full --track-origins=yes ./build/test_file_rotate
          valgrind --error-exitcode=1 --leak-check=full --track-origins=yes ./build/test_pipeline

  cppcheck:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install cppcheck
        run: sudo apt-get update && sudo apt-get install -y cppcheck

      - name: Run cppcheck
        run: cppcheck --enable=all --error-exitcode=1 --inline-suppressions include/ core/ sinks/
